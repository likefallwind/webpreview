## Update Plan

## 现状：

1. 目前网站是静态的，只有前端页面，且内容是写死的

## 希望升级的目标：

1. 网站一部分内容可以依然保持静态，另一部分页面是动态的
2. 具体来说：
   1. 静态的页面包括：首页和实验室介绍（index.html），加入我们（contact.html）
   2. 动态的页面包括：新闻动态（news.html），学术论文（research.html），研究成果（projects.html），团队成员（team.html）
3. 最终目标，是做一个前后端都有的网站。前端样式保持不变，后端围绕让这些动态页面可以根据内容变化显示就可以
4. 不需要管理端。需要做一个导表脚本，自动把info_list内容更新到后端就可以。动态更新的方式，是使用数据库，还是直接js更新前端内容，你来决定。
5. 本网站是实验室官网，所以不会有很大的并发访问之类，简单一点就行


## 动态页面规则：

1. 所有信息已经放到info_list.xlsx里，你需要仔细检查一下这个表格。这里面有几个动态页面要用的全部信息，按照sheet来区分。同时每个sheet第一行对相关属性的标题，你需要检查属性并和网站位置配对，后面其他行，为具体的值
2. 具体来说，按照info_list.xlsx的sheet名字：
   1. 新闻动态_news：对应新闻动态（news.html）。这里面包括时间、标题和内容
   2. 学术论文_research：对应学术论文（reasearch.html）。这里面包括论文作者、会议/期刊名称，会议时间，原文链接（如果是空白，表示没有链接）
   3. 研究成果_projects：对应研究成果（projects.html）。这里面包含项目名称、logo图片、简介和链接
   4. 团队成员_教师_team：对应团队成员（team.html）。这里面主要是团队成员里面，老师的部分，包括姓名、照片、职称、简介、邮箱和个人主页。注意这个和后面的，团队成员_学生_team是放到同一个页面里面，老师放在前面，学生放在后面。前端布局就采用team.html的，所以请查看以后再代码实现
   5. 团队成员_学生_team：对应团队成员（team.html）。这里面主要是团队成员里面，学生的部分，包括姓名、照片、职称、简介、邮箱和个人主页。注意这个和前面的，团队成员_老师_team是放到同一个页面里面，老师放在前面，学生放在后面。前端布局就采用team.html的，所以请查看以后再代码实现

## 额外注意：

1. 在修改代码前，请仔细阅读本说明，相关代码和info_list的部分，并且要明确要修改的对应关系
2. 前端样式已经生成，注意不要修改前端样式
3. 这里如果有不确定的地方，请及时提问